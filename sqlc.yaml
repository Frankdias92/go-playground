version: "2"
sql:
  - engine: "postgresql"
    queries: "./sql"
    schema: "./sql/migrations"
    database:
      uri: postgresql://postgres:postgres@localhost:5432/postgres_db
    gen:
      go:
        package: "postgres_db"
        out: "./sql"
        sql_package: "pgx/v5"